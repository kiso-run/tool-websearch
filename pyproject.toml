[project]
name = "skill-search"
version = "0.1.0"
description = "Web search skill for Kiso"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28",
]

[dependency-groups]
dev = ["pytest>=8"]

[tool.uv]
managed = true

[tool.pytest.ini_options]
pythonpath = ["."]
